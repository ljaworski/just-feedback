# Image tag published at ghcr.io/ljaworski/just-feedback.
JF_IMAGE_TAG=latest

# Host port mapped to container port 4180.
JF_PUBLISHED_PORT=4180

# Set to true only when requests arrive through a trusted reverse proxy.
JF_TRUST_PROXY=false
